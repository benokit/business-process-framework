# definitions

Schema elements defining the shapes of core framework building blocks — `element`, `schema`, `service`, `method`, `execution-node`, `execution-node-template`, `function`, `pure-function`, and `app-config`.

These definitions are context-independent: they describe structure only, with no runtime behaviour attached.
